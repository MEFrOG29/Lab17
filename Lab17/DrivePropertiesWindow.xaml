<Window x:Class="Lab17.DrivePropertiesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab17"
        mc:Ignorable="d"
        Title="DrivePropertiesWindow" Height="350" Width="400">
    <StackPanel Margin="20">
        <StackPanel Orientation="Horizontal" Margin="0, 0, 0, 15">
            <Image Source="/Resources/drive.png" Width="50" Height="50"/>
            <TextBlock Text="{Binding Name}" FontSize="18" VerticalAlignment="Center" Margin="15, 0, 0, 0"/>
        </StackPanel>

        <Separator Margin="0, 0, 0, 15"/>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Файловая система: " Margin="0,5"/>
            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding FileSystem}" Margin="5,5"/>

            <TextBlock Grid.Row="1" Text="Занято: " Margin="0, 15, 0, 5" Foreground="Red"/>
            <StackPanel Grid.Row="1" Grid.Column="1" Margin="5, 15, 0, 5">
                <TextBlock Text="{Binding UsedBytes, StringFormat='\{0:N0\} байт'}"/>
                <TextBlock Text="{Binding UsedGb}" FontWeight="Bold"/>
            </StackPanel>

            <TextBlock Grid.Row="2" Text="Свободно: " Margin="0,5" Foreground="Green"/>
            <StackPanel Grid.Row="2" Grid.Column="1" Margin="5,5">
                <TextBlock Text="{Binding FreeBytes, StringFormat='\{0:N0\} байт'}"/>
                <TextBlock Text="{Binding FreeGb}" FontWeight="Bold"/>
            </StackPanel>

            <Separator Grid.Row="3" Grid.ColumnSpan="2" Margin="0,10"/>

            <TextBlock Grid.Row="4" Text="Емкость: " Margin="0,5"/>
            <StackPanel Grid.Row="4" Grid.Column="1" Margin="5,5">
                <TextBlock Text="{Binding TotalBytes, StringFormat='\{0:N0\} байт'}"/>
                <TextBlock Text="{Binding TotalGb}" FontWeight="Bold"/>
            </StackPanel>
        </Grid>

        <Button Content="OK" Width="80" HorizontalAlignment="Right" Margin="0, 20, 0, 0" Click="Button_Click"/>
    </StackPanel>
</Window>
